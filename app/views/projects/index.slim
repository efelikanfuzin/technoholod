.content._projects
  .breadcrumbs
    = link_to 'Главная', root_path
    span.arrow >
    h1.last Проекты

  - for project in @projects
    = link_to project, class: 'blog-item' do
      - unless project.preview_img.thumb.blank?
        .blog-img
          = image_tag project.preview_img.thumb.url

      .blog-item_content
        h3.blog-item_title = project.title
        p.text_block = project.preview

  .cf
    = paginate @projects
